<h1>Tell us about the good deed that you were inpsired to do</h1>

<p>You were inspired by <%= link_to "this deed.", good_path(@parent) %></p>

<%= form_for(@good) do |f| %>
  <% if @good.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@good.errors.count, "error") %> prohibited this good from being saved:</h2>

      <ul>
      <% @good.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :desc, "Description of your good deed, maximum of 120 characters" %><br />
    <%= f.text_area :desc, :id => 'description' %>
  </div>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.hidden_field :parent_id, :value => @parent.id %>
  <div class="actions">
    <%= f.submit "Record Good Deed", :class => 'btn' %>
  </div>
<% end %>

<%= link_to 'Back', goods_path %>